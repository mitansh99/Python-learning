# Numpy is a python lib used for working with arrays.
# it has also fun for working in domain for linear algebra, fourier transform, and matices
# NumPy stands for Numerical python

# NumPy -> python have list == arrays but they are slow to process

# NumPy is 50x fister then traditional python list

# object in NumPy is called ndarray 

# store at one continues place 
